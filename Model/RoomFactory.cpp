#include "RoomFactory.h"

Room* RoomFactory::makeRoom(std::string type) const{
    Room* room;
    room = createRoom(type);
    //TODO finish
}

Room* RoomFactory::createRoom(std::string type) const{
    //TODO
}